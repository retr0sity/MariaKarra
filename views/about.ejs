<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Maria Karra</title>
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <!-- Custom CSS -->
        <link rel="stylesheet" href="css/styles.css">
    <style>
        /* Custom styles for the header and about section */
        .hero-section {
            position: relative;
            height: 70vh;
            background-image: url('images/kremala.jpg');
            background-size: cover;
            background-position: center;
            color: white;
        }
        .hero-section::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1;
        }
        .hero-content {
            position: relative;
            z-index: 2;
            top: 40%;
            left: 40%;
            transform: translate(-50%, -50%);
            text-align: left;
            max-width: 50%;
            padding: 2rem;
            border-radius: 10px;
        }
        .hero-content h1 {
            font-size: 2.5rem;
        }
        .hero-content p {
            font-size: 1.8rem;
        }
        .link-section {
            text-align: center;
            margin-top: 2rem;
        }
        .link-section a {
            margin: 0 15px;
            font-weight: bold;
            color: black;
            text-decoration: none;
            cursor: pointer;
            position: relative;
        }
        
        .link-section a::before {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 50%;
            width: 0;
            height: 2px;
            background-color: black;
            transition: width 0.3s ease, left 0.3s ease;
        }
        .link-section a:hover::before, .link-section a.active::before {
            width: 100%;
            left: 0;
        }
        .content-section {
            margin-top: 2rem;
            text-align: center;
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <div class="hero-section">
        <%- include("partials/header.ejs") %>
            <div class="hero-content">
            <h1>Maria Karra</h1>
            <p>"Maria Karra is a contemporary artist blending fine arts, game design, and interdisciplinary research. Her work explores the intersections of art, culture, and technology, focusing on narrative-driven experiences and experimental design."</p>
        </div>
    </div>

    <!-- Links Section -->
    <div class="link-section">
        <a id="exhibitions-link">Exhibitions</a>
        <a id="publications-link">Publications</a>
    </div>

    <!-- Content Section -->
    <div class="content-section" id="content-section">
        <!-- Dynamic content will be loaded here -->
    </div>

    <%- include("partials/footer.ejs") %>

   <!-- Bootstrap Icons -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
   <!-- Bootstrap JS -->
   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
   <script src="js/app.js"></script>
   <script>
       document.getElementById('exhibitions-link').addEventListener('click', function() {
           document.getElementById('content-section').innerHTML = '<h2>Exhibitions</h2><p>List of exhibitions...</p>';
           setActiveLink(this);
       });

       document.getElementById('publications-link').addEventListener('click', function() {
           document.getElementById('content-section').innerHTML = '<h2>Publications</h2><p>List of publications...</p>';
           setActiveLink(this);
       });

       function setActiveLink(element) {
           var links = document.querySelectorAll('.link-section a');
           links.forEach(function(link) {
               link.classList.remove('active');
           });
           element.classList.add('active');
       }
   </script>
</body>
</html>